# Render Blueprint – Norya web servisi
# PDF (WeasyPrint) için cairo/pango gerekir → Dockerfile kullanın.
# Dashboard'da mevcut servis "Python" ile oluşturulduysa: Settings → Build & Deploy → Environment → Docker'a geçin (veya yeni servis Blueprint ile Docker ile oluşturun).

services:
  - type: web
    name: norya
    runtime: docker
    # Dockerfile kökte: python:3.12-slim + cairo/pango (WeasyPrint)
    # buildCommand/startCommand kullanılmaz; Dockerfile CMD çalışır. PORT Render tarafından verilir.
    # İsteğe bağlı: Render PostgreSQL eklediyseniz bağlayın
    # - key: DATABASE_URL
    #   fromDatabase:
    #     name: norya-db
    #     property: connectionString
